# spendback PRD

> 상태: 초안 v0.9 · 2026-09-25 · 기획 인터뷰로 합의한 초기 기획에 1단계 검증(기기, 모델 파일), LLM 스파이크, 부트스트랩 결과를 반영하고 고정비 관리를 추가. 디자인 시스템 연동(기기 확인 전)과 DESIGN.md 작성, 디자인 시스템 0.3.0 교체를 반영. 11장 미결 사항을 정했다(탭 3개, 예산 이어 쓰기, 소비 속도와 일할, 비교 기준, 백업, 조사, 하네스 기준). 에뮬레이터 확인을 반영
>
> 기획의 정본은 이 문서다. 앱의 디자인과 디자인 시스템 연동은 `docs/DESIGN.md`(12장 3번에서 작성)에 따로 둔다. 결정은 사용자와 합의한 것이므로, 바꿀 이유가 보이면 근거를 붙여 제안하고 합의한 뒤 반영한다. 결정의 이유는 13장에 있다.

## 1. 개요

spendback은 온디바이스 LLM이 소비 회고를 써 주는 Android 가계부다. 지출을 폼으로 빠르게 기록하면, 한 주나 한 달이 끝났을 때 코드가 소비 지표를 계산하고 로컬 LLM이 그 지표로 회고 문장을 쓴다. 데이터와 모델은 모두 기기 안에만 있다.

- 사용자: 개발자 본인 1명(Galaxy S24+)
- 형태: React Native CLI(New Architecture) Android 앱, 포트폴리오 프로젝트
- 언어·통화: 한국어, 원화(원 단위 정수)

### 포트폴리오로 보여줄 것

1. **온디바이스 LLM 통합**: 모델 다운로드, 로드, 스트리밍, 해제
2. **LLM과 코드의 경계 설계**: LLM이 숫자를 만들 수 없는 구조를 만들고, 평가 하네스로 증명
3. **제품·UX 완성도**: 3번 탭으로 끝나는 입력, 읽히는 회고

## 2. 핵심 원칙: 계산은 코드가, 문장 생성과 입력 구조화는 LLM이

| 역할 | 담당 | 적용 시점 |
|---|---|---|
| 합산, 비율, 증감, 예산 대비, 날짜 계산, 금액 표기 | 코드 | MVP |
| 어떤 사실을 골라 어떤 순서로 말할지, 연결 문장, 제안 | LLM | MVP(회고) |
| 비정형 텍스트를 거래로 구조화 | LLM + 코드 검증 | 2단계(카드 알림 파싱) |

코드가 계산해 LLM에 넘기는 지표 묶음을 **facts**라 부른다. 원칙을 지키는 장치는 네 가지다.

- LLM은 숫자를 직접 쓰지 않는다. 금액, 비율, 횟수, 증감 표현은 전부 `{키}` 플레이스홀더로 참조하고, 값은 코드가 채운다. 사용자가 이름에 숫자를 넣을 수 있으므로 카테고리와 이유 태그의 이름도 플레이스홀더로 참조한다.
- llama.rn의 출력 문법(GBNF)으로 문장 문자를 한글 음절과 일부 문장부호로 한정해, 숫자와 facts에 없는 키를 원천 차단한다.
- 문법으로 막을 수 없는 한글 수사("삼만 원", "두 배")는 코드가 사후 검사하고, 걸리면 재생성한다.
- 직전 기간 데이터가 없으면 비교 키를 facts에 넣지 않는다. 그러면 LLM이 비교 문장을 쓸 수 없다.

## 3. 범위

### MVP

- 폼 입력(지출·수입), 내역 편집·삭제
- 카테고리·이유 태그 편집
- 달력 월 예산(월 총예산 + 선택형 카테고리 예산). 한 번 정하면 다음 달에도 이어 쓴다
- 고정비 관리(항목 등록 + 달마다 기록 여부를 확인하는 체크리스트)
- 주간·월간 회고(지표, 차트, LLM 문장)
- 모델 다운로드·교체
- JSON 내보내기·가져오기
- LLM 평가 하네스(PC 로컬 스크립트)

### 2단계 이후(우선순위 순)

1. **카드 알림 파싱**: Android `NotificationListenerService` 네이티브 모듈로 알림을 받고, LLM이 거래로 구조화하고, 사용자가 폼에서 확인한 뒤 저장한다. 결제 취소 알림이 오면 해당 거래를 삭제한다. 핵심 원칙의 "입력 구조화"가 처음 적용되는 기능이다.
2. **다짐 루프**: 회고 끝에 다음 기간 다짐을 남기고, 다음 회고에서 코드가 달성 여부를 판정한다.
3. **대화형 질의**: "이번 달 배달 얼마 썼어?" 같은 질문을 LLM이 질의로 구조화하고, 코드가 계산한다.
4. **보안**: 앱 잠금(생체 인증), DB 암호화(SQLCipher)

### 범위 밖

- 자연어 입력, 영수증 OCR, 음성 입력, CSV 가져오기
- iOS, 서버·동기화·계정, 다국어·다중 통화
- 할부, 반복 거래 자동 생성. 고정비는 자동으로 만들지 않고 체크리스트로 기록을 돕는다(4.4)

## 4. 주요 기능

### 4.1 입력(하단 시트 폼)

목표는 금액 → 카테고리 → 저장, 3번 탭이다.

| 필드 | 필수 | 규칙 |
|---|---|---|
| 금액 | 필수 | 숫자 키패드. 원 단위 양의 정수 |
| 카테고리 | 필수 | 칩 선택. 지출/수입 전환에 따라 목록이 바뀜 |
| 날짜 | 기본값 오늘 | "어제" 칩, 그 외는 날짜 선택기 |
| 지출/수입 | 기본값 지출 | 접힌 영역 |
| 이유 태그 | 선택 | 1개. 지출만 |
| 만족도 | 선택 | 후회 / 보통 / 만족. 지출만 |
| 메모 | 선택 | 과거 메모 자동완성. 같은 메모에 전에 고른 카테고리를 자동 선택(코드) |
| 결제수단 | 선택 | 카드 / 현금 / 계좌이체 |
| 고정비 | 기본값 끔 | 월세·구독 등. 회고에서 변동비와 분리. 켜면 등록한 고정비 항목을 골라 연결할 수 있다(4.4). 지출만 |

### 4.2 카테고리와 이유 태그

- 지출 카테고리 기본 11개: 식비, 카페·간식, 배달, 교통, 쇼핑, 생활, 주거·통신, 문화·여가, 의료·건강, 경조사·선물, 기타
- 수입 카테고리 기본 3개: 급여, 부수입, 기타. 월급은 급여, 부업·주식처럼 월급 밖의 수입은 부수입으로 적는다
- 이유 태그 기본 6개: 필요, 충동, 보상·스트레스, 약속·사교, 습관, 선물
- 모두 추가와 이름 변경이 가능하다. 삭제는 숨김으로 처리해 과거 거래와 집계를 보존한다.

### 4.3 예산

- 기준 기간은 달력 월(1일~말일) 하나다. 시작일 설정은 없다. 월급처럼 한 달에 한 번 들어오는 돈으로 한 달을 사는 것을 전제로 한다.
- 월 총예산을 두고, 카테고리별 예산은 선택이다.
- 예산은 한 번 정하면 다음 달에도 그대로 이어 쓴다. 바꾸면 바꾼 달부터 적용되고 지난 달의 예산은 그대로 남는다. 지난 달의 예산은 고치지 않는다(지난 회고의 기준이 바뀌지 않게).
- 예산은 변동비에만 적용한다. 고정비는 예산 사용률에 넣지 않는다. 수입(부수입 포함)은 예산을 늘리지 않는다.
- 주간 예산은 해당 주의 날짜마다 "그 달 예산 ÷ 그 달 일수"를 더해 구한다. 두 달에 걸친 주도 같은 규칙을 쓰고, 카테고리 예산도 같다.
- 주의 하루라도 예산이 없으면(첫 예산을 정한 달보다 앞선 날) 그 주의 총예산 지표를 facts에 넣지 않는다. 카테고리 예산도 카테고리마다 같아서, 두 달에 걸친 주에서 한 달에만 그 카테고리 예산이 있으면 그 카테고리의 주간 예산 지표를 뺀다. 반쪽 예산과 한 주 지출을 비교하면 사용률이 부풀려진다(2장의 "데이터가 없으면 키를 넣지 않는다"와 같다).
- 홈에서는 진행 중인 달의 예산 소진율과 남은 일수 대비 소비 속도를 보여준다. 계산은 아래와 같고 금액은 원 단위로 내린다.

| 값 | 식 |
|---|---|
| 사용액 | 이번 달 1일부터 오늘까지의 변동비 지출 합계(오늘 기록 포함) |
| 남은 예산 | 월 총예산 − 사용액. 0보다 작으면 초과 상태이고 초과액을 보여준다 |
| 남은 일수 | 말일 − 오늘 + 1. 오늘은 아직 쓸 수 있는 날이라 포함한다 |
| 하루 쓸 수 있는 금액 | 남은 예산 ÷ 남은 일수. 초과 상태에서는 보여주지 않는다 |
| 지난 비율(게이지 기준선) | 오늘 ÷ 그 달 일수. 오늘이 끝날 때까지의 몫이라 1일에도 0이 아니다 |
| 사용률 | 사용액 ÷ 월 총예산. 지난 비율보다 크면 빠르게 쓰고 있는 것이다 |

### 4.4 고정비

월세, 통신비, 구독처럼 매달 나가는 지출을 항목으로 등록해 두고, 달마다 기록했는지 확인한다. 목적은 입력 편의보다 누락 방지다(13장).

- 항목에는 이름, 예상 금액, 지출 카테고리, 결제일(매월 N일), 결제수단(선택)을 둔다.
- 주기는 매월 하나다. 연 단위 결제(보험 연납 등)는 항목으로 두지 않고, 결제한 달에 고정비를 켜서 입력한다.
- 결제일이 그 달에 없으면(2월 30일 등) 그 달의 말일로 본다.
- 등록 전에 이미 지난 결제일을 미기록으로 잡지 않도록, 항목은 등록한 날(당일 포함) 이후의 결제일부터 확인한다.
- 해지하면 항목을 숨긴다. 숨긴 항목은 확인하지 않고, 4.2와 같이 과거 거래와 집계는 보존한다.
- 거래를 자동으로 만들지 않는다(13장). 예상 금액은 입력 폼의 기본값으로만 쓰고, 지표와 예산은 기록된 거래로만 계산한다.

**체크리스트**

- 한 달 안에 항목에 연결된 거래(`fixedCostId`)가 1건 이상 있으면 그 달은 기록한 것으로 본다.
- 홈에는 이번 달 기록 현황("고정비 3/5 기록")과, 결제일이 오늘이거나 지났는데 기록하지 않은 항목을 보여준다.
- 항목을 누르면 입력 폼이 채워진 채로 열린다. 금액·카테고리·결제수단은 항목 값, 메모는 항목 이름, 날짜는 그 달의 결제일이다. 사용자는 금액을 확인하고 저장하며, 저장한 거래는 그 항목에 연결된다.
- 입력 폼에서 고정비를 켜고 항목을 골라도 연결된다(4.1).
- 끝난 달의 월간 회고를 생성하기 전에, 그 달에 기록하지 않은 항목이 있으면 먼저 알린다(4.6).

### 4.5 내역

날짜별로 묶은 목록, 필터(기간 / 카테고리 / 태그 / 지출·수입), 편집·삭제.

### 4.6 회고

**기간**: 주간(월~일)과 월간(달력 월). 비교 대상인 직전 기간은 바로 앞 주, 바로 앞 달이다.

**생성 조건**

- 끝난 기간만 생성한다. 진행 중인 기간은 지표와 차트만 보여준다.
- 기간 안의 변동비 지출 기록이 3건 미만이면 LLM을 부르지 않고 "기록이 부족해요"라고 안내한다.
- 끝난 기간의 회고 상세를 처음 열 때 생성해 저장한다. 저장본이 있으면 저장본을 보여주고, 재생성은 사용자가 요청할 때만 한다.
- 월간 회고는 생성하기 전에(저장본이 없거나 재생성을 요청했을 때) 그 달에 기록하지 않은 고정비 항목이 있는지 확인한다(4.4). 있으면 그 항목을 보여주고, 사용자는 기록하거나 그대로 생성할 수 있다.
- 한 기간에 회고는 1개만 둔다. 재생성에 성공하면 기존 회고를 덮어쓴다.
- 생성은 스트리밍으로 보여주고, 생성 중 취소를 지원한다. 스트리밍 단위는 문장 필드(`headline`, 각 `insights` 항목, `suggestion`)다. 필드 하나가 완성될 때마다 사후 검사를 하고, 통과한 필드만 플레이스홀더를 채워 화면에 내보낸다. 사후 검사에 걸리면 화면에 나간 문장을 지우고 "다시 쓰는 중" 상태로 전체를 재생성한다.

**facts(코드 계산)**

"대상" 열은 지표를 계산할 때 쓰는 지출이다. 고정비는 월세처럼 특정 날짜에 몰려 주간 비교와 최대 단일 지출 같은 지표를 왜곡하므로, 총지출과 수입 대비 지출률을 뺀 지표는 변동비만 쓴다.

| 지표 | 대상 | 주간 | 월간 |
|---|---|---|---|
| 총지출(전체, 변동비, 고정비를 따로) | 전체 | O | O |
| 직전 기간 대비 증감액·증감률 | 변동비 | O | O |
| 카테고리별 합계·비중, 증감 상위 3개 | 변동비 | O | O |
| 예산 대비 사용률, 초과·잔여 금액(총예산·카테고리 예산) | 변동비 | O(일할 예산) | O |
| 이유 태그별 합계·비중 | 변동비 | O | O |
| "후회" 만족도가 붙은 지출 합계 | 변동비 | O | O |
| 최대 단일 지출 | 변동비 | O | O |
| 무지출일 수(변동비 지출이 없는 날) | 변동비 | O | O |
| 지출이 가장 많은 요일 | 변동비 | O(가장 많이 쓴 날) | O |
| 같은 메모가 반복된 지출 상위 3개(메모 완전 일치 기준) | 변동비 | O | O |
| 수입 대비 지출률(그 달에 수입 기록이 있을 때만) | 전체 | - | O |

회고는 끝난 기간만 다루므로 "남은 일수 대비 소비 속도"는 회고 지표가 아니라 홈 지표다.

- 직전 기간과 비교하는 지표(증감액·증감률, 증감 상위 3개)는 직전 기간 전체가 첫 기록일(가장 이른 거래의 날짜) 이후일 때만 facts에 넣는다. 직전 기간이 기록을 시작한 날과 겹치면 없음으로 본다. 반쪽 기간과 비교하면 증감이 부풀려진다.
- 증감 상위 3개는 증감액의 절댓값이 큰 순이다. 같으면 이번 기간 금액이 큰 순, 그다음 카테고리 순서다. 증감이 0인 카테고리는 넣지 않는다.
- 직전 금액이 0이면 증감률 키를 넣지 않는다(비율을 정할 수 없다). 증감액은 넣는다.

**출력 형식**

LLM은 아래 JSON만 출력한다. `insights`는 2~4개다.

```json
{ "headline": "...", "insights": ["...", "..."], "suggestion": "..." }
```

- 문장 안의 수치, 증감, 카테고리·이유 태그 이름은 플레이스홀더로만 쓴다. 이름 키는 표시 이름이 아니라 id로 만든다(`{category.<id>.name}`). 예를 들어 LLM이 `{category.<id>.name} 지출이 {category.<id>.change_phrase}. 전체 지출의 {category.<id>.share}를 차지했어요.`라고 쓰면, 코드가 `배달 지출이 32,000원 늘었어요. 전체 지출의 18%를 차지했어요.`로 채운다.
- 증감 방향도 계산 결과이므로 "늘었어요 / 줄었어요 / 같았어요"까지 코드가 채운다.
- 요청할 때마다 facts의 키 목록으로 GBNF 문법을 만든다. 문장에 쓸 수 있는 문자는 화이트리스트(한글 음절 `가-힣`, 공백, `.,!?·~()-`)로 한정한다. 숫자를 빼는 블랙리스트 방식은 다른 문자 체계의 숫자와 기호로 우회된다(13장).
- headline과 각 insight에는 플레이스홀더를 1개 이상 넣도록 문법으로 강제한다.
- insight마다 먼저 사실 묶음 하나를 고르게 하고(출력에 묶음 이름을 함께 쓴다), 그 문장에는 그 묶음의 키만 쓰도록 문법을 만든다. 다른 사실의 키를 붙이는 오용(10장)을 구조로 막는다.
- 플레이스홀더 바로 뒤의 조사는 LLM이 쌍으로 쓰고(`{이/가}`, `{을/를}`, `{은/는}`, `{와/과}`, `{으로/로}`, `{이에요/예요}`, `{이었어요/였어요}` 등) 코드가 채운 값의 마지막 글자 받침으로 고른다(`으로/로`는 ㄹ 받침도 `로`). 숫자와 기호로 끝나는 값은 읽는 소리로 판단한다(`18%`는 "퍼센트"라 받침이 없다). 문법은 플레이스홀더 바로 뒤에 맨 조사를 쓰지 못하게 한다. 스파이크의 "54,000원였어요" 같은 오류를 구조로 막는다.
- 사후 검사에서 한글 수사와 단위의 조합("삼만 원", "두 배", "세 번" 등)이 나오면 재생성한다. 재시도는 최대 2회이고, 그래도 실패하면 폴백으로 넘어간다.
- 금액은 어디서나 `32,000원` 형식으로 표기하고, 비율은 정수 %로 표기한다. 반올림 규칙은 코드에 두고 단위 테스트로 고정한다.

**저장**: facts 스냅샷, 출력 JSON, 모델 ID, 프롬프트 버전, 생성 시각을 함께 저장한다. 과거 회고는 목록에서 다시 열람할 수 있다.

**톤**: 담백한 분석가. 마지막에 제안 한 줄을 붙인다.

**폴백**: 문장을 만들 수 없으면 지표와 차트만 보여주고, 원인에 따라 다르게 안내한다.

| 원인 | 안내 | 동작 |
|---|---|---|
| 모델이 없음 | "모델을 받으면 회고 문장이 생성됩니다" | 모델 관리로 이동 |
| 모델 로드 실패 | "모델을 불러오지 못했어요" | 다시 시도, 모델 관리로 이동 |
| 사후 검사 반복 실패 | "회고 문장을 만들지 못했어요" | 다시 생성 |

폴백 상태는 회고로 저장하지 않는다. 다음에 상세를 열면 다시 생성을 시도한다.

### 4.7 모델 관리(설정)

- 레지스트리에 등록된 모델 목록을 보여주고, 다운로드, 삭제, 사용 모델 선택을 지원한다.
- 다운로드는 Hugging Face에서 받는다. 진행률 표시, 이어받기, SHA-256 검증, 저장 공간 확인을 붙이고 Wi-Fi 사용을 권장한다.
- 개발 빌드에는 기기의 로컬 파일 경로에서 모델을 로드하는 옵션을 둔다.

### 4.8 백업

JSON 내보내기·가져오기. 파일은 Android 공유 시트로 주고받는다. 가져올 때 스키마 버전을 확인하고 Zod로 검증한다. 고정비 항목도 함께 담고, 거래의 `fixedCostId` 연결을 유지한다.

가져오기는 병합하지 않고 전체 교체한다. 확인 대화상자를 거치고, 파일 전체를 먼저 검증한 뒤 한 트랜잭션으로 바꿔서 도중에 실패하면 기존 데이터가 그대로 남는다. 교체 직전의 데이터는 앱 안에 한 벌(가장 최근 것만) 보관해, 잘못 가져왔을 때 백업 화면에서 한 번 되돌릴 수 있다.

## 5. 화면 구성

탭은 3개다. 설정은 탭이 아니라 홈 헤더 오른쪽의 톱니 버튼으로 여는 화면이다.

| 탭 | 내용 |
|---|---|
| 홈 | 이번 달 예산 게이지(총예산·카테고리), 소비 속도, 고정비 체크리스트, 최근 지출, 입력 버튼(하단 시트 폼을 연다). 헤더 오른쪽에 설정 |
| 내역 | 목록, 필터, 편집·삭제 |
| 회고 | 주간/월간 목록 → 상세(스트리밍 문장, 카테고리 도넛 차트, 일별 막대 차트) |

설정 화면에는 카테고리·태그 편집, 예산, 고정비 항목, 모델 관리, 백업을 둔다. 예산은 홈 게이지와 첫 실행 버튼, 고정비는 홈 고정비 줄, 모델 관리는 회고 폴백에서도 들어간다.

## 6. LLM 설계

### 런타임

- llama.rn 0.12.x를 정확한 버전으로 고정한다. npm `latest` 태그가 RC 버전을 가리키고 있어서 `^` 범위 지정을 쓰지 않는다.
- 대상 기기는 국내판 Galaxy S24+(SM-S926N, Exynos 2400, `ro.soc.model=s5e9945`, RAM 12GB, Android 16)다(2026-09-24 adb 확인). CPU 추론이 전제다. GPU(Xclipse 940)에는 Vulkan 1.3과 OpenCL 드라이버가 있지만, llama.rn 0.12.9의 Android 빌드에는 Vulkan 백엔드가 없다. OpenCL·Hexagon 빌드는 Qualcomm 기기로 판별될 때만 불러오므로, 이 기기에서는 CPU 라이브러리 `rnllama_jni_v8_2_dotprod_i8mm`가 로드된다.
- 2026-09-24 스파이크에서 세 후보 모델이 모두 로드됐다(`qwen35` 포함). 측정 앱과 결과는 `spike/llm-runtime/`(README.md)에 있다. 앱과 독립된 버릴 코드라 앱의 타입 검사·린트·포맷·테스트·번들 대상에서 제외했다.
- Gemini Nano는 사용자의 S24+에서 쓸 수 없다(사용자 확인).

### 모델 레지스트리

| 모델 | GGUF 저장소 | 라이선스 | 비고 |
|---|---|---|---|
| Qwen3.5-2B | `unsloth/Qwen3.5-2B-GGUF`(비공식) | Apache-2.0 | 잠정 기본값. 공식 Qwen 조직에는 GGUF가 없다. 표준 Q4_K_M이지만 unsloth 보정 데이터로 만든 imatrix를 써서 다른 양자화본과 품질이 다를 수 있다(`UD-` 접두어의 Dynamic 2.0 파일과는 다르다). 저장소의 비전용 `mmproj-*.gguf`는 받지 않는다 |
| Kanana-1.5-2.1B | `DevQuasar/kakaocorp.kanana-1.5-2.1b-instruct-2505-GGUF`(비공식) | Apache-2.0 | 한국어 특화. 공식 kakaocorp 조직에는 GGUF가 없어 제3자 변환본만 있다. v0.2에 적었던 `Grit-Labs/kanana-1.5-2.1b-instruct-2505-GGUF`(2026-09-11 생성)는 이 저장소의 파일을 그대로 다시 올린 것이다(SHA-256 동일). 직접 변환도 검토한다 |
| EXAONE 4.0 1.2B | `LGAI-EXAONE/EXAONE-4.0-1.2B-GGUF`(공식) | EXAONE AI Model License Agreement 1.2 - NC(비상업, 재배포 금지) | 개인용. 앱은 Hugging Face에서 받기만 한다 |

**파일(Q4_K_M)**

| 모델 | 파일명 | 크기(바이트) | SHA-256 | 아키텍처 | 커밋 |
|---|---|---|---|---|---|
| Qwen3.5-2B | `Qwen3.5-2B-Q4_K_M.gguf` | 1,280,835,840 | `aaf42c8b7c3cab2bf3d69c355048d4a0ee9973d48f16c731c0520ee914699223` | `qwen35` | `f6d5376be1edb4d416d56da11e5397a961aca8ae` |
| Kanana-1.5-2.1B | `kakaocorp.kanana-1.5-2.1b-instruct-2505.Q4_K_M.gguf` | 1,522,796,768 | `24d3db59d0af2c85c0afc0bbc99da1174b73ef6728bce2650bd91bec28ad1c81` | `llama` | `4d3b6203857d893ebfcaca6c51901e3ea1d00d00` |
| EXAONE 4.0 1.2B | `EXAONE-4.0-1.2B-Q4_K_M.gguf` | 812,437,792 | `7b5e753540183ae4d56e6febd9b48cdd944de53386e6faa8f51c8f98cb2b47df` | `exaone4` | `162446400ea4596377a3ce1d3ddffa32971af0a6` |

2026-09-24에 아래 파일 페이지와 API(`/api/models/<저장소>/tree/<커밋>`), 다운로드 응답 헤더(`x-linked-size`, `x-linked-etag`)로 확인했고 세 값이 일치했다. 아키텍처는 GGUF 헤더의 `general.architecture`이고, 세 파일 모두 `general.file_type`이 Q4_K_M(15)이다. 세 아키텍처 모두 llama.rn 0.12.9의 `cpp/llama-arch.cpp`에 있다.

- https://huggingface.co/unsloth/Qwen3.5-2B-GGUF/blob/f6d5376be1edb4d416d56da11e5397a961aca8ae/Qwen3.5-2B-Q4_K_M.gguf
- https://huggingface.co/DevQuasar/kakaocorp.kanana-1.5-2.1b-instruct-2505-GGUF/blob/4d3b6203857d893ebfcaca6c51901e3ea1d00d00/kakaocorp.kanana-1.5-2.1b-instruct-2505.Q4_K_M.gguf
- https://huggingface.co/LGAI-EXAONE/EXAONE-4.0-1.2B-GGUF/blob/162446400ea4596377a3ce1d3ddffa32971af0a6/EXAONE-4.0-1.2B-Q4_K_M.gguf

앱은 커밋을 고정한 `https://huggingface.co/<저장소>/resolve/<커밋>/<파일명>`으로 받는다. 저장소의 main이 바뀌어도 받는 파일과 SHA-256이 어긋나지 않는다.

기본 모델은 Qwen3.5-2B다(2026-09-25 잠정 결정). 평가 하네스로 확인하고, 기준에 못 미치면 다른 후보와 비교해 바꾼다(11장).

### 실행 정책

- 회고 상세 화면에서 문장을 생성해야 할 때(저장본이 없거나 재생성을 요청했을 때)만 로드하고, 화면을 나가거나 앱이 백그라운드로 가면 해제한다. 회고 목록, 진행 중인 기간, 기록이 부족한 기간, 저장본을 여는 경우에는 로드하지 않는다.
- 추론 모드(thinking)는 끄고, 스트리밍과 취소를 지원한다. Qwen3.5는 추론 모드를 꺼도 출력 앞에 빈 `<think></think>`가 붙으므로 떼고 파싱한다.
- 스레드는 6개로 둔다(스파이크에서 2·4·6·8개 중 가장 빨랐다). 컨텍스트 길이와 샘플링은 평가 하네스로 정한다(11장).
- llama.rn 0.12.9의 `ignore_eos`와 `logit_bias`는 쓰지 않는다. 비운 벡터에 토큰 id를 인덱스로 써서 앱이 SIGSEGV로 죽는다(`cpp/jsi/JSIParams.cpp:629`). 버전을 올릴 때 다시 확인한다.

### 인터페이스

- 회고 문장 생성은 `narrate(facts)` 인터페이스 뒤에 둔다. 구현은 llama.rn 구현과 가짜 구현 두 가지이고, 도메인·화면 테스트는 가짜 구현으로 돌린다.
- 프롬프트는 버전으로 관리하고, 회고 스냅샷에 그 버전을 기록한다.

### 평가 하네스(PC 로컬 스크립트, CI 아님)

- node-llama-cpp로 폰과 같은 GGUF 파일, 프롬프트, GBNF 문법을 쓴다. Windows와 macOS 양쪽에서 돈다.
- 입력은 고정된 facts 스냅샷 약 20개다. 직전 기간 없음, 기록 적음, 예산 초과, 고정비 비중이 큼 같은 경계 사례를 포함한다.
- 자동 점검 항목: 스키마 통과, 참조한 키가 facts에 있는지, 숫자·한글 수사가 없는지, 문장 길이, 금지어
- 문장 길이 한도(플레이스홀더를 채운 뒤, 공백 포함): headline 30자, insight 60자, suggestion 45자. headline과 suggestion은 한 문장, insight는 두 문장까지다. S24+에서 본문 한 줄이 약 22자라 headline은 두 줄(xl), insight는 세 줄, suggestion은 두 줄에 맞춘 값이다
- 금지어 초안: 느낌표, 인사·호칭("안녕하세요", "여러분", "고객님"), 단정·과장("항상", "절대", "완벽", "최고", "최악", "대박", "심각"), 비난·훈계("낭비", "과소비", "한심", "반성", "해야 해요"), 사실 밖 추측("것 같아요", "나 봐요", "듯해요")
- 길이 한도와 금지어는 첫 하네스 결과를 보고 조정한다
- 사람 채점 항목: 자연스러움, 고른 사실이 적절한지, 톤. 채점 기준표를 둔다.
- 결과로 모델 비교표를 만들어 README에 싣는다.

## 7. 데이터 모델(초안)

금액은 원 단위 정수다. 날짜는 기기 로컬 시간 기준 `YYYY-MM-DD` 문자열이고, 한 주의 시작은 월요일이다.

| 엔티티 | 주요 필드 |
|---|---|
| Transaction | id, type(expense·income), amount, date, categoryId, reasonTagId?, satisfaction?(regret·neutral·satisfied), memo?, paymentMethod?(card·cash·transfer), isFixed, fixedCostId?(isFixed인 거래만), createdAt, updatedAt |
| Category | id, type(expense·income), name, sortOrder, hidden, isDefault |
| ReasonTag | id, name, sortOrder, hidden, isDefault |
| Budget | effectiveFrom(`YYYY-MM`), total, categoryBudgets(categoryId → amount). 한 달의 예산은 effectiveFrom이 그 달 이하인 것 중 가장 늦은 것이다. 예산을 바꾸면 그 달을 effectiveFrom으로 저장한다(4.3) |
| FixedCost | id, name, amount(예상 금액), categoryId(지출), dayOfMonth(1~31), paymentMethod?, sortOrder, hidden, createdAt, updatedAt |
| Retrospective | id, kind(weekly·monthly), periodStart, periodEnd, facts, output, modelId, promptVersion, createdAt. (kind, periodStart)마다 1개 |
| Model(앱에 내장한 레지스트리) | id, repo, commit, fileName, sizeBytes, sha256, license. 값은 6장 레지스트리 표를 따른다 |
| 설정(MMKV) | 사용 모델 ID, 다운로드한 모델 목록 등 |

## 8. 기술 스택

버전은 2026-09-24 조사 기준이다. 착수할 때 최신 버전을 다시 확인한다.

| 영역 | 선택 |
|---|---|
| 코어 | React Native 0.87.x CLI(New Architecture 전용), TypeScript, Hermes V1(RN 0.87은 항상 켜져 있어 설정하지 않는다), React Compiler 1.0(Babel 플러그인) |
| 패키지 매니저 | pnpm 10(`node-linker=hoisted`), Node ^22.13(`.nvmrc`는 22.23.3). pnpm은 스파이크에서 검증했고 옆 폴더 프로젝트와 같은 메이저인 10을 쓴다 |
| DB | op-sqlite + drizzle-orm / drizzle-kit(마이그레이션) |
| 키-값 저장소 | react-native-mmkv 4(Nitro) |
| 화면 이동 | React Navigation 7 정적 API |
| 상태 | Zustand(UI 상태만 관리하고, 데이터의 기준은 DB) |
| 차트 | victory-native + react-native-skia + Reanimated 4. Reanimated 4.7.0과 react-native-worklets 0.13.0은 NativeWind(react-native-css)가 필요로 해 12장 3번에서 먼저 넣었다 |
| 스타일링 | 개인 디자인 시스템 `@eeennsu/native`(NativeWind 5 기반). 0.3.0(2026-09-25 npm 배포, RN 컴포넌트 11개: Button, Input, Textarea, Label, Card, Badge, Chip, Icon, Text, Stack, Box)을 Expo 없이 연동했다. nativewind 5.0.0-preview.4 · react-native-css 3.0.7은 DS peer대로 정확한 버전이고, Tailwind 4와 react-native-svg(DS 아이콘)를 함께 넣었다. react-native-css의 Metro 변환기는 `@expo/metro-config`에 기대므로 같은 일을 하는 앱 변환기(`metro.transformer.js`)로 바꿨다. 다크 모드는 OS를 따르고, 앱 색은 global.css에서 DS의 semantic 색 변수만 다시 선언한다. 연동 방식과 앱 디자인은 `docs/DESIGN.md` |
| LLM | llama.rn 0.12.x(정확한 버전 고정) |
| 검증 | Zod 4(LLM 출력 스키마, 백업 가져오기) |
| 테스트 | Jest 29 + React Native Testing Library 14, E2E는 Maestro. `@react-native/jest-preset` 0.87이 Jest 29 기준이다. RNTL이 쓰는 `test-renderer`는 1.2.0에 고정한다(1.3.0은 react 19.3 이상을 요구하는데 RN 0.87.1은 react 19.2.3이다). 디자인 시스템 컴포넌트의 스타일은 global.css를 `react-native-css/jest`에 등록해 확인한다(DESIGN.md 1.5) |
| 도구 | ESLint 9(flat config), Prettier, husky, lint-staged, commitlint. ESLint 9는 지원이 끝났지만 `eslint-plugin-react`와 `eslint-plugin-react-native`가 10을 지원하지 않아 유지한다. 두 플러그인이 10을 지원하면 올린다 |

Android만 대상이므로 Windows와 macOS 양쪽에서 개발·빌드할 수 있다. Android 패키지명은 `com.eeennsu.spendback`이고, 64비트 ABI(arm64-v8a, x86_64)만 빌드한다.

**참고할 옆 폴더 프로젝트**

- `../expo-plate`: pnpm, husky, commitlint, CI, `.claude/rules` 구성을 참고한다. Expo 기반이라 템플릿으로 쓰지는 않는다.
- `../rn-upgrade-kit`: RN 업그레이드 스킬 모음이다. Expo를 쓰지 않는다는 전제가 spendback과 맞는다. 그중 `rehearsal`은 Windows에서 실행을 거부하므로 macOS에서 쓴다.
- `../design-system`: `@eeennsu/{tokens,web,native}`의 원본이다. native는 NativeWind와 react-native-css를 peer로 정확히 고정하고, react와 RN은 범위(react ≥19, RN ≥0.81)로 받는다. 원본 레포에서 로컬 링크로 검증할 때는 react·RN 버전이 정확히 같아야 한다(`../design-system/CLAUDE.md`). spendback에 필요해 만든 0.3.0(Chip, Icon, RN 눌림 표시·누름 영역·입력 포커스, 번들 크기 줄이기)은 2026-09-25에 publish했고 spendback이 쓴다(DESIGN.md 5.2).

## 9. 테스트 전략

- **단위(Jest)**: 지표 계산기, 예산 일할과 이어 쓰기, 홈 소비 속도, 고정비 체크리스트 판정(말일, 등록일 경계), 날짜와 주 경계, 금액 포맷터, 플레이스홀더 렌더러(조사 고르기 포함), GBNF 생성기, 한글 수사 검사기, 백업 스키마. 핵심 원칙을 지키는 부분이라 TDD로 만든다.
- **컴포넌트(RNTL 14)**: 입력 폼의 3번 탭 흐름, 회고 화면의 상태(생성 중 / 완료 / 폴백 / 기록 부족 / 고정비 미기록 알림)
- **E2E(Maestro)**: 입력 → 내역 → 회고 열람
- **LLM**: 평가 하네스(6장)

## 10. 위험과 가정

| 위험·가정 | 대응 |
|---|---|
| CPU 추론만 가능해 느리다. 스파이크(스레드 6)에서 첫 회고의 headline까지 Qwen 8.6~11.3초, Kanana 13.7~18.7초, EXAONE 6.2~8.5초, 전체는 18~31초가 걸렸다. 대부분 약 600~850토큰 프롬프트 처리 시간이다. 모델을 올린 동안 메모리는 PSS 기준 2.0~3.3GB다 | facts 표현을 압축해 프롬프트를 줄이고, 생성 중 상태를 보여준다 |
| 2B급 모델이 플레이스홀더를 다른 사실의 키에 붙이거나("야식 치킨 지출이 {배달의 증감}"), 단위("20,100원원")와 서술어("늘었어요가 늘었어요")를 중복하고, 조사를 틀린다("54,000원였어요"). 숫자를 지어내는 것만 막힐 뿐 틀린 사실은 만들 수 있다 | 문법으로 insight마다 한 사실 묶음의 키만 쓰게 하고, 조사는 쌍으로 쓰게 해 코드가 고른다(4.6). 평가 하네스 자동 점검에 키 오용, 단위 중복, 조사를 넣는다 |
| llama.rn 0.12.9의 `ignore_eos`·`logit_bias`가 SIGSEGV를 낸다 | 쓰지 않고, 버전을 올릴 때 다시 확인(6장) |
| 연속으로 추론하면 약 4분 만에 열 상태가 3(severe)까지 오른다 | 회고는 기간마다 한 번 생성하므로 영향이 작다. 하네스는 PC에서 돈다 |
| Windows에서 llama.rn 설치·빌드가 깨진다: SDK CMake 3.22.1의 ninja가 긴 경로를 지원하지 않고(`LongPathsEnabled=1`이어도 260자 제한), pnpm 10이 설치 스크립트를 막고, Git Bash의 GNU tar가 `C:\` 경로를 원격 호스트로 읽는다 | llama.rn의 CMake `buildStagingDirectory`를 짧은 경로로 옮기고, `pnpm.onlyBuiltDependencies`에 `llama.rn`을 넣고, Windows에서는 PowerShell에서 설치한다. 앞의 둘은 부트스트랩에서 미리 넣었다(`android/build.gradle`, `package.json`) |
| node-llama-cpp와 llama.rn에 들어간 llama.cpp 버전이 달라 평가 결과와 기기 결과가 어긋날 수 있음. 2026-09-24 기준 llama.rn 0.12.9는 build 10256(`6c8dcaa`), node-llama-cpp 3.21.1은 v0.4.0(2026-09-12 스냅샷)이라 약 5~6주 차이가 나고, `qwen35`와 sampler 코드가 다르다(grammar는 거의 같다) | 두 버전을 기록하고, 기기에서 표본을 교차 확인 |
| 디자인 시스템이 NativeWind 5.0.0-preview.4와 react-native-css 3.0.7에 고정돼 있다(2026-09-24 상위 최신은 5.0.0-rc.0과 3.1.0-rc.0). 앱 Metro 변환기는 react-native-css의 컴파일러와 주입 형식에 기대므로 react-native-css를 올리면 깨질 수 있다. 개발 중에는 다른 파일에 더한 새 클래스가 global.css 내용을 바꾸거나 Metro를 다시 켜기 전까지 반영되지 않는다(내용 없이 다시 저장하면 안 된다). 연동은 Jest, Metro 번들, 에뮬레이터(2026-09-25, Android 16, S24+ 해상도)로 확인했고 실기기에서는 아직 보지 않았다 | DS가 NativeWind를 정식 승격 뒤 한 번 올릴 때(DS 스펙 C-19) 변환기와 DESIGN.md 1.4를 함께 확인한다. 기기 확인 목록은 DESIGN.md 1.6이다 |
| GBNF로는 한글 수사를 막을 수 없음 | 사후 검사 + 재생성 + 폴백 |
| 2B급 모델의 한국어 문장 품질. 스파이크(모델당 3회)에서는 Kanana가 가장 자연스러웠고 EXAONE이 가장 장황하고 형식을 자주 벗어났다 | 평가 하네스로 모델을 비교하고 대안 모델로 교체 |

## 11. 미결 사항

2026-09-25에 사용자와 정했다. 결과는 해당 장과 13장에 반영했고, 평가 하네스로 확인할 두 가지만 남긴다.

| 항목 | 확인 시점 | 지금 값 |
|---|---|---|
| 기본 모델 | 평가 하네스 결과 | Qwen3.5-2B(잠정). 기준에 못 미치면 다른 후보와 비교해 바꾼다 |
| llama.rn 파라미터(컨텍스트 길이, 샘플링) | 평가 하네스 | n_ctx 4096. 샘플링은 스파이크에서 쓴 값(temperature 0.7, top_k 20, top_p 0.9)과 Qwen3.5 모델 카드의 비추론 권장값(temperature 1.0, top_p 1.0, top_k 20, min_p 0, presence_penalty 2.0)을 같은 facts로 비교해 자동 점검 통과율, 사람 채점 순으로 고른다 |

## 12. 작업 순서(기본안)

UI는 디자인 시스템 연동과 DESIGN.md가 준비된 뒤에 만들고, 그 전에는 UI와 무관한 계층을 먼저 만든다. 3번(디자인 시스템 쪽)은 앱의 4·5번과 나란히 진행할 수 있다.

1. **LLM 스파이크**: S24+에서 llama.rn과 후보 모델의 로드 시간, 토큰 속도, 메모리, 플레이스홀더 GBNF 동작을 측정한다. 가장 큰 불확실성이라 가장 먼저 한다. (2026-09-24 완료. 결과는 6장, 10장, 11장, 13장에 반영했다)
2. **프로젝트 부트스트랩**: RN CLI, pnpm, TypeScript, 린트, Jest (2026-09-24 완료. 릴리스 빌드와 S24+에서 디버그 빌드 실행(Metro 연결)까지 확인했다)
3. **디자인 시스템 연동과 DESIGN.md**: 부트스트랩한 앱에 npm의 `@eeennsu/native`를 설치해 Expo 없이 기기에서 띄우고 연동 방식을 정한다. 앱마다 갈리는 축(색, 간격 리듬, 화면 구조)과 필요한 컴포넌트를 `docs/DESIGN.md`에 정의하고, 디자인 시스템에 없는 컴포넌트는 디자인 시스템을 개선해 배포한다 (2026-09-25 소스 작업 완료. 0.2.0을 연동해 Jest와 Metro 번들로 확인했고, DESIGN.md와 홈·입력 시트 시안을 만들었다. DS에 없던 것은 0.3.0으로 만들어 publish했고, spendback을 올려 임시 컴포넌트를 걷어 냈다. 2026-09-25 에뮬레이터(Android 16, S24+ 해상도)에서 확인했다. 남은 일: 실기기 확인(DESIGN.md 1.6))
4. **도메인 계층**: 거래·카테고리·예산·고정비 스키마(drizzle), 지표 계산기, 예산 일할, 고정비 체크리스트 판정, 포맷터(TDD)
5. **회고 파이프라인**: facts → GBNF 생성 → `narrate` → 사후 검사 → 렌더러, 평가 하네스
6. **UI**(3번이 끝난 뒤): 입력 폼, 홈, 내역, 회고, 설정
7. **마무리**: 모델 관리, 백업, E2E

## 13. 결정 기록

| 결정 | 이유 |
|---|---|
| 입력은 폼만 둔다(자연어 입력 제외) | 금액과 카테고리 같은 정형 입력은 폼이 자연어보다 빠르고, 자연어 파서는 구현 난이도만 높인다. 입력 구조화는 비정형 텍스트가 들어오는 카드 알림 파싱에서 보여주는 편이 더 설득력 있다 |
| GBNF의 문장 문자는 화이트리스트로 정한다 | 2026-09-24 스파이크에서 숫자만 뺀 블랙리스트 문법은 9회 중 5회가 `۱۱۲`(아랍·페르시아 숫자), `⅔`, `₁`, `㎡`, `％`로 숫자를 썼고, `ㄴ만` 같은 자모나 태국어·한자·영어로도 빠져나갔다. 한글 음절과 일부 문장부호만 허용한 문법에서는 9회 모두 숫자와 외국 문자가 없었다 |
| 숫자는 플레이스홀더로만 쓴다 | 사후 검증과 달리 "LLM은 숫자 문자를 쓸 수 없다"가 문법으로 구조적으로 보장되고, 테스트로 증명할 수 있다. 문법으로 막을 수 없는 한글 수사는 사후 검사와 재생성으로 막는다. 증감 방향도 계산 결과라서 코드가 쓴다 |
| 기준 기간을 달력 월 하나로 통일한다 | 월간 회고를 달력 기준으로 정했기 때문에, 예산 주기와 어긋나지 않도록 시작일 설정을 없앴다 |
| 완전 로컬로 만든다 | "금융 데이터가 기기 밖으로 나가지 않는다"가 로컬 LLM의 설득력이다. 서버를 붙이면 범위가 두 배가 된다 |
| Android만 지원한다 | 사용자 본인 폰(S24+) 전용이다 |
| 런타임은 llama.rn | New Architecture를 네이티브로 지원하면서 JSON 스키마·GBNF 제약 디코딩을 하는 사실상 유일한 크로스 플랫폼 런타임이다. Gemini Nano는 대상 기기에서 쓸 수 없고, ExecuTorch는 제약 디코딩이 없다 |
| 회고는 끝난 기간만 만든다 | 진행 중인 데이터로 결론을 내리지 않는다 |
| 폴백은 지표와 차트만 보여준다 | 템플릿 문장 생성기를 따로 만들면 문장 생성기가 두 벌이 된다 |
| 금액 표기를 `32,000원`으로 통일한다 | 화면마다 표기가 달라지지 않게 한다 |
| React Native 0.87.x를 유지한다 | 2026-09-24 기준 0.88은 RC(0.88.0-rc.2)이고 정식 버전은 0.87.1이다. 0.88이 정식 출시되고 패치가 나온 뒤 `../rn-upgrade-kit`로 올린다 |
| 고정비는 대부분의 회고 지표와 예산에서 뺀다 | 월세처럼 특정 날짜에 몰리는 지출이 주간 비교, 최대 단일 지출, 예산 일할을 왜곡한다 |
| 고정비는 항목을 등록하고 달마다 체크리스트로 기록을 확인한다 | 고정비를 빠뜨리면 회고의 고정비 합계와 수입 대비 지출률이 틀리고, 회고는 저장본으로 남아 재생성하기 전까지 틀린 채로 있다. 입력 편의보다 누락 방지가 목적이다 |
| 반복 거래를 자동으로 만들지 않는다 | 앱이 꺼져 있을 때 만들려면 백그라운드 작업이나 밀린 달을 소급해 만드는 로직이 필요하다. 통신비·공과금처럼 금액이 달마다 바뀌는 항목은 자동으로 넣은 값이 틀린다. 2단계 카드 알림 파싱과 겹쳐 카드로 나가는 구독이 두 번 기록된다. 체크리스트 방식은 알림으로 들어온 거래를 항목에 연결하는 식으로 확장할 수 있다 |
| 탭은 홈·내역·회고 3개로 두고 설정은 홈 헤더에서 연다 | 설정 안의 기능은 대부분 다른 곳에서도 들어간다(예산은 홈 게이지와 첫 실행 버튼, 고정비는 홈 고정비 줄, 모델 관리는 회고 폴백). 탭에는 매일 돌아오는 곳만 남긴다(DESIGN.md 7.3) |
| 예산은 한 번 정하면 다음 달에도 이어 쓰고, 바꾸면 그 달부터 적용한다 | 월급처럼 매달 같은 돈으로 사는 것이 전제다. 매달 다시 정하게 하면 잊은 달에 예산이 빈다. 바꾼 달부터만 적용해야 지난 회고의 기준이 바뀌지 않는다. 달마다 값을 복사하는 대신 적용 시작 달을 저장하면 복사 시점을 정할 필요가 없다 |
| 예산이 없는 날이 낀 주는 주간 예산 지표를 뺀다 | 반쪽 예산과 한 주 지출을 비교하면 사용률이 부풀려진다. 첫 예산을 정한 직후나 카테고리 예산을 새로 둔 직후의 한 주에만 생기는 일이라, 날짜를 맞춰 비율을 다시 계산하는 규칙을 따로 두지 않는다 |
| 하루 쓸 수 있는 금액은 오늘을 포함한 남은 일수로 나눈다 | 오늘은 아직 쓸 수 있는 날이다. 게이지 기준선은 오늘이 끝날 때까지의 몫으로 그어 1일에도 0이 되지 않게 한다 |
| 직전 기간은 전체가 첫 기록일 이후일 때만 비교한다 | 기록을 시작한 주나 달과 비교하면 증감이 부풀려진다 |
| 조사는 LLM이 쌍으로 쓰고 코드가 고른다 | 채운 값의 받침은 코드만 안다. 스파이크에서 "54,000원였어요"처럼 틀렸다 |
| 백업 가져오기는 전체 교체하고, 교체 직전 데이터를 한 벌 보관한다 | 병합하려면 같은 거래를 판별할 기준이 필요한데 id가 기기마다 달라 중복이 생긴다. 잘못 고른 파일로 몇 달치 기록이 사라지지 않게 한 번 되돌릴 수 있게 한다 |
| 기본 모델은 Qwen3.5-2B로 두고 평가 하네스로 확인한다 | 스파이크에서 세 후보가 모두 로드됐다. Qwen은 속도가 EXAONE 다음이고 라이선스가 Apache-2.0이다. 한국어 자연스러움은 Kanana가 나았으므로 하네스에서 비교한다 |
| facts 키는 표시 이름이 아니라 id로 만든다 | 사용자가 이름을 바꾸거나 이름에 숫자를 넣어도 키가 흔들리지 않는다(2장). 키 이름 규칙 표는 회고 파이프라인을 만들 때 코드와 함께 둔다 |
