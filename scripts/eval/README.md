# 평가 하네스

회고 문장 생성(PRD 4.6)을 PC에서 모델별로 돌려 비교한다(PRD 6장). 폰과 같은 GGUF 파일, 프롬프트, GBNF 문법, 사후 검사를 쓴다. 앱의 `narrate`(`src/retro/narrate.ts`)를 그대로 부르고, 모델을 부르는 부분만 llama.rn 대신 node-llama-cpp다. CI가 아니라 PC에서 손으로 돌린다.

## 실행

Node 22.13 이상. Windows와 macOS 모두 돈다.

```sh
pnpm eval:models                       # GGUF 세 개(약 3.6GB)를 받아 SHA-256을 확인한다
pnpm eval                              # 스냅샷 20개 × 모델 3개 × 1회
pnpm eval qwen3.5-2b --runs 3          # 모델 하나, 스냅샷마다 3회
pnpm eval --only week-base,month-base  # 스냅샷 일부
pnpm eval --temp 0.3 --repeat 1        # 샘플링 비교(기본값은 src/retro/models.ts의 INFERENCE)
```

모델은 `~/.cache/spendback/models`(Windows는 `%USERPROFILE%\.cache\spendback\models`)에 받는다. 다른 폴더는 `--dir <폴더>`로 준다. 결과는 `results/<시각>.json`(시도별 원문 포함)과 `results/<시각>.md`(모델 비교표, 사람 채점용 문장)다.

## 입력

`snapshots.ts`의 facts 스냅샷 20개. 스파이크와 같은 한 주(`week-base`)를 기준으로 직전 기간 없음, 변동비 3건, 예산 초과·없음, 고정비가 큼, 감소, 직전 0원, 한 카테고리 몰림, 고르게 씀, 태그·후회 없음, 이름에 숫자·영문, 무지출일 많음, 카테고리 예산 초과, 증감 0, 월간(수입 있음·적자·수입 없음·첫 달)을 담는다.

## 자동 점검

앱의 사후 검사(`src/retro/check.ts`)가 걸러 재생성하는 것과, 완료한 회고에 하네스가 더 보는 것으로 나뉜다.

| 항목 | 어디서 | 내용 |
|---|---|---|
| 읽기 실패 | 앱 | JSON 스키마(Zod)를 통과하지 못했다 |
| 모르는 키 | 앱 | facts에 없는 키, 고른 묶음 밖의 키 |
| 수사 | 앱 | 문자 화이트리스트 밖(숫자, 다른 문자 체계), 한글 수사와 단위("삼만 원", "두 배", "세 번") |
| 단위 | 앱 | 숫자 없이 남은 단위("지출이 원 늘었어요"), 값 뒤에 또 쓴 단위("1일 일") |
| 방향 | 앱 | 증감을 서술어 키 없이 직접 쓴 말(늘었·줄었·증가·감소…) |
| 되풀이 | 앱 | 같은 글자를 세 번 넘게 이음("했어요요요") |
| 길이 초과 | 하네스 | 채운 뒤 headline 30자, insight 60자, suggestion 45자(공백 포함). S24+ 본문 한 줄이 약 22자다 |
| 문장 수 초과 | 하네스 | headline·suggestion 한 문장, insight 두 문장 |
| 금지어 | 하네스 | 느낌표, 인사·호칭, 단정·과장, 비난·훈계("~야 해요"), 사실 밖 추측(`checks.ts`) |
| 같은 묶음 | 하네스 | insight끼리 같은 묶음을 고름 |

길이 한도와 금지어는 PRD 6장의 초안이고, 결과를 보고 조정한다.

## 사람 채점

`results/<시각>.md`의 "문장" 절에서 회고마다 세 항목을 1~3점으로 매긴다.

| 항목 | 3점 | 2점 | 1점 |
|---|---|---|---|
| 자연스러움 | 사람이 쓴 듯한 해요체 | 어색한 낱말이나 어순이 한두 곳 | 뜻이 통하지 않는 문장이 있다 |
| 사실 선택 | 그 기간에 가장 눈에 띄는 사실을 골랐다 | 사실은 맞지만 덜 중요한 것을 골랐다 | 값을 엉뚱한 뜻으로 썼다("수입이 지출보다 112% 많았어요") |
| 톤 | 담백한 분석가, 제안이 할 수 있는 일이다 | 조금 과장되거나 막연하다 | 훈계하거나 단정한다 |

## 알려진 차이

- PC에서는 Metal·CUDA로 돌아 속도가 폰(CPU, 스레드 6)과 다르다. 시간은 참고만 한다.
- node-llama-cpp 3.21.1과 llama.rn 0.12.9에 든 llama.cpp 버전이 다르다(PRD 10장). 문법 처리는 거의 같다.
- Kanana GGUF의 Jinja 템플릿은 node-llama-cpp의 검사를 통과하지 못해 같은 형식의 Llama 3.1 래퍼로 돈다. llama.rn은 GGUF 템플릿을 그대로 쓴다.
- Qwen3.5는 node-llama-cpp에서 `thoughts: 'discourage'`로 추론 모드를 끈다(빈 think 블록을 프롬프트에 넣는다). `budgets.thoughtTokens: 0`으로 끄면 문법이 고른 첫 토큰 `{`가 think 구간에 들어가 사라진다.

## 결과

### 2026-09-26 첫 전체 실행

[results/2026-09-26T07-56-10.md](results/2026-09-26T07-56-10.md). 프롬프트 v1, 온도 0.7, top-k 20, top-p 0.9, 반복 벌점 1.1, 스냅샷 20개 × 2회, M4 Mac(Metal).

| 모델 | 완료 | 첫 시도 통과 | 평균 시도 | 첫 문장(초) | 전체(초) | 수사 | 단위 | 방향 | 되풀이 | 길이 초과 | 문장 수 초과 | 금지어 | 같은 묶음 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Qwen3.5-2B | 36/40 | 23/40 | 1.63 | 4.4 | 7.3 | 5 | 7 | 3 | 18 | 52 | 0 | 2 | 0 |
| Kanana-1.5-2.1B | 38/40 | 25/40 | 1.48 | 5.2 | 8.6 | 11 | 4 | 9 | 1 | 67 | 1 | 4 | 0 |
| EXAONE 4.0 1.2B | 39/40 | 24/40 | 1.52 | 3.0 | 6.1 | 17 | 7 | 2 | 3 | 99 | 5 | 3 | 7 |

- 구조는 지킨다. 세 모델 모두 숫자를 지어내지 못하고(모르는 키·읽기 실패 0), 폴백까지 간 회고는 120개 중 7개다.
- 문장 품질은 셋 다 아직 쓸 수 없다. Qwen은 짧지만 어색하고("늘 들어와요 21% 늘었어요"), Kanana는 길고 조각난 문장을 남기며("비중을 차지했어요 17%를"), EXAONE은 프롬프트의 "기간"을 옮기고 습니다체로 길게 쓴다. 길이 한도를 넘는 문장이 많다.
- 문법으로 막은 증감 낱말을 띄어 써서 우회한다("늘 었어요"). 금지 낱말을 늘리는 방식은 한계가 있다.
- 값을 엉뚱한 뜻으로 쓰는 오류가 남는다("수입이 지출보다 112%를 더 많았어요", 카테고리 이름 뒤에 총 증감률). 문법은 키의 묶음까지만 막고, 글자로 쓴 이름이 값을 잘못 가리키는 것은 막지 못한다.

이 결과는 이 표까지만 싣고, 다음 방향은 PRD 11장에서 정한다.

### 조정 기록

같은 날 Qwen으로 스냅샷 8개 × 2회를 돌리며 문법과 사후 검사를 조였다(커밋 c97cf23).

| 바꾼 것 | 완료 | 첫 시도 통과 |
|---|---|---|
| 처음(부호 허용, 문장 조각 길이 상한) | 6/16 | 2/16 |
| 온도 0.3 | 3/16 | 2/16 |
| 낱말 단위 문장, 단위 글자 금지, 서술어 뒤 마침표(반복 벌점 없음) | 11/16 | 5/16 |
| 반복 벌점 1.1(1.2는 8/16, 5/16) | 9/16 | 9/16 |
| 증감 낱말 금지 | 16/16 | 15/16 |
| 문장 하나에 묶음 하나 | 16/16 | 16/16 |
